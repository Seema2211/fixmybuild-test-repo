# fixmybuild-test-repo

Test repository for [FixMyBuild AI](https://github.com/Seema2211/fixmybuild-test-repo). The `TestApp` project is intentionally broken (references `Newtonsoft.Json` without the package) so that the GitHub Actions build fails and FixMyBuild AI can detect and analyze the failure.
